
spring:
  application:
    name: ProyecTuritsExplor

  datasource:
    url: jdbc:postgresql://turistexplor.c788u86imhde.us-east-2.rds.amazonaws.com:5432/postgres_bd
    username: postgres
    password: turistexplor
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

  servlet:
    multipart:
      max-file-size: 2000MB
      max-request-size: 2000MB

security:
  jwt:
    expiration-in-minutes: 60